const maxEventPrice = 5000;
const minEventPrice = 0;
const priceSliderStep = 5;

const baseUrl = 'https://localhost:7107';

export {
    maxEventPrice,
    minEventPrice,
    priceSliderStep,
    baseUrl
}