import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h4>Internship project for the "Modsen" company❤️</h4>
    </footer>
  );
};

export default Footer;
